# ==============================================================================
# RIDE CONNECT - FRONTEND (.env.compose)
# Modelo / Exemplo de configuracao para execucao via Docker Compose
# ==============================================================================

# URL da API quando acessada atraves do proxy reverso Nginx na porta 8080
VITE_API_BASE_URL=http://localhost:8080/api

# Chave da API do Google Maps / Routes
VITE_API_GOOGLE_API_KEY=sua_chave_google_aqui
